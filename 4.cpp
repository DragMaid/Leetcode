class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
        int i = 0, in = nums1.size();
        int j = 0, jn = nums2.size();
        int cnt = 0;
        while (i < in || j
    }
};
